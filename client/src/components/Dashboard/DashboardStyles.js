import styled from "styled-components";
import { colors } from "../../styledComponents/GenericStyles";



export const Container = styled.div``;

export const Nav = styled.nav`
    background-color: ${colors.azulObscuro} !important;
`;